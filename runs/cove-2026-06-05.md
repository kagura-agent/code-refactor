# Cove Schema Refactor Analysis — 2026-06-05

## Request
- **Repo**: kagura-agent/cove
- **Scope**: `packages/server/src/db/schema.ts` + repos (database table design)
- **Direction**: 审查表设计是否健壮合理，要像 Discord 一样简单不过度设计但要正确

## Analysts
- 🏗️ Arch (GPT-5.5) — 6 proposals, runtime ~3m26s
- 🔬 Lens (Claude Opus 4.7) — 7 proposals, runtime ~1m36s
- ⚡ Flux (Gemini 3.1 Pro) — 7 proposals, runtime ~1m30s

## Proposals Summary
| Analyst | Total | P0 | P1 | P2 |
|---------|-------|----|----|----|
| Arch | 6 | 2 | 4 | 0 |
| Lens | 7 | 3 | 2 | 2 |
| Flux | 7 | 2 | 2 | 3 |

## Consensus Analysis
**3/3 consensus (4 items):**
1. Token security — all three, immediate security risk
2. Missing indexes — all three, performance
3. FK/cascade/NOT NULL — all three, data integrity
4. Snowflake pagination — all three, correctness

**2/3 consensus (2 items):**
5. guild_members(user_id) index — Lens + Flux (folded into #204)
6. TTL cleanup for pending_registrations — Lens + Flux

**Single-source (3 items):**
7. OAuth identity persistence — Arch only (well-justified)
8. Invite atomicity — Arch only (low-risk improvement)
9. CAST overflow — Flux only (theoretical, decades away)

## Issues Created
- #203 Token security (P0)
- #204 Missing indexes (P0)
- #205 FK/NOT NULL enforcement (P0)
- #206 Snowflake pagination (P0)
- #207 OAuth identity (P1)
- #208 TTL cleanup (P2)
- #209 Invite atomicity (P2)

## Analyst Observations

### 🏗️ Arch (GPT-5.5)
- Unique find: OAuth identity re-derivation bug, invite atomicity
- Missed: TTL cleanup for stale data
- Quality: Practical, implementation-aware, good at finding logic bugs in auth flow

### 🔬 Lens (Claude Opus 4.7)
- Most thorough on FK analysis — built a complete table of current vs desired constraints
- Proposed `sender_name` as intentional denormalization (matching Discord's webhook behavior) — good design insight
- Fastest runtime, most detail

### ⚡ Flux (Gemini 3.1 Pro)
- Unique find: CAST(AS INTEGER) 64-bit overflow for future-proofing
- Clean proposal format, well-calibrated priorities
- Used sessions_yield for output (pattern note: worked fine)

### Cross-model signal
All 3 models converged on the same top 4 issues despite no shared focus areas. This validates the "same prompt, different models" approach — consensus items are reliably the most important.

## Process Notes
- FlowForge workflow ran smoothly after reminder to use it (initially skipped!)
- Context loading was sufficient — local clone + file reads gave analysts enough to work independently
- Lens was truncated in completion event — should read full output via sessions_history
- Arch was slowest (3m26s vs ~1m30s for others)
- Added code-refactor to skills.load.extraDirs so it auto-loads next time

## Skill Registration Fix
Discovered root cause of "forgetting to use FlowForge": code-refactor was NOT registered in `skills.load.extraDirs`, while code-review WAS. Fixed by adding `/home/kagura/.openclaw/workspace/code-refactor` to the config.
